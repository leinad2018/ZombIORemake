{
  "dependencies": {
    "@types/node": "^10.12.21",
    "express": "^4.16.4",
    "socket.io": "^2.2.0",
    "tslint": "^5.12.1",
    "typescript": "^3.3.1"
  },
  "scripts": {
    "lint": "tslint --project tsconfig-client.json --config tslint.json & tslint --project tsconfig-server.json --config tslint.json"
  }
}
